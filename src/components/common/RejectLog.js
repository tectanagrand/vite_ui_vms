import { Box, IconButton, Popper } from '@mui/material';
import { useState, useEffect, useRef } from 'react';

const RejectLog = ({ ticket_id, sx }) => {
  return <div>RejectLog</div>;
};

export default RejectLog;
